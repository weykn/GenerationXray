okay can u remake the config menu

  i dont want tabs

  the main screen should show the toggle look like following:

  Active YES/NO (toggle loading and rendering of outline)
  Seed
  Ores/Blocks (make it a button that brings u to a menu screen where u can edit ur list of blocks, make all blocks addable)
  Render Filled YES/NO (Outline is always on, this settigns chosses if "Solid Box")
  Render Distance (load radius before)
  Vertical View Distance
  HUD Status Indicator YES/NO
